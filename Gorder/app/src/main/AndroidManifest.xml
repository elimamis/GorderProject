<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.elimamis.gorder" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_gorder"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".FirstScreenActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChooseTableActivity"
            android:label="@string/title_activity_main_activity2" />
        <activity
            android:name=".SignUpActivity"
            android:label="SignUpActivity" />
        <activity
            android:name=".MainActivityPage"
            android:label="@string/title_activity_main" />
        <activity
            android:name=".MyListViewMain"
            android:label="@string/title_activity_my_list_view_main" >
        </activity>
        <activity android:name=".MenuListActivity"

            android:theme="@style/AppTheme.menusTheme">

        </activity>

    </application>

</manifest>
